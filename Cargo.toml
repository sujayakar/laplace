[workspace]
members = ["spike", "host", "shared"]
exclude = ["guest"]
resolver = "2"

# guest is built separately with --target aarch64-unknown-none, not part of the
# default workspace build (different target triple).
