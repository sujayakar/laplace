[package]
name = "convex-runner-js"
version = "0.1.0"
edition = "2021"

[dependencies]
rquickjs = { version = "0.9" }
libc = "0.2"

[profile.release]
opt-level = "s"
lto = true
strip = true
panic = "abort"
