[package]
name = "convex-runner-js"
version = "0.1.0"
edition = "2021"

[dependencies]
boa_engine = { version = "0.21", default-features = false }
boa_runtime = "0.21"
libc = "0.2"

[profile.release]
opt-level = "s"
lto = true
strip = true
panic = "abort"
